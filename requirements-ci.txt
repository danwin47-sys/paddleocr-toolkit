# CI/CD Testing Dependencies (不包含 paddlepaddle 以避免 CPU 相容性問題)
# GitHub Actions 測試環境專用 - 相容 Python 3.8+

# ============ PDF Processing ============
pymupdf>=1.23.0  # 降低版本以支援 Python 3.8
pdf2image>=1.16.0
pillow>=9.0.0

# ============ Image Processing ============
opencv-python>=4.5.0
numpy>=1.21.0,<2.0.0  # Python 3.8 相容

# ============ Translation ============
requests>=2.31.0
tqdm>=4.65.0
ollama>=0.1.0; python_version >= "3.9"  # Python 3.9+

# ============ Web Framework (optional but for coverage) ============
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.5
websockets>=11.0
pydantic>=1.10.0,<3.0.0  # 相容範圍更廣
httpx>=0.24.0  # Required for starlette.testclient

# ============ CLI Enhancement ============
rich>=12.0.0
pyyaml>=6.0.0

# ============ Data Handling ============
python-docx>=0.8.11
markdown>=3.4.0

# ============ Development Dependencies ============
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-env>=0.8.0
pytest-mock>=3.10.0
pytest-flask>=1.2.0
pytest-asyncio>=0.23.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.0.0

# ============ PaddleOCR Mock (用於測試) ============
# 在 CI 環境中，我們 mock paddleocr，所以不需要真正安裝
