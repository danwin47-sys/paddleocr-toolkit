# CI/CD Testing Dependencies (不包含 paddlepaddle 以避免 CPU 兼容性問題)
# GitHub Actions 測試環境專用 - 兼容 Python 3.8+

# ============ PDF Processing ============
pymupdf>=1.23.0  # 降低版本以支援 Python 3.8
pdf2image>=1.16.0
pillow>=9.0.0

# ============ Image Processing ============
opencv-python>=4.5.0
numpy>=1.21.0,<2.0.0  # Python 3.8 兼容

# ============ Translation ============
ollama>=0.1.0; python_version >= "3.9"  # Python 3.9+

# ============ Web Framework (optional but for coverage) ============
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.5
websockets>=11.0
pydantic>=1.10.0,<3.0.0  # 兼容範圍更廣

# ============ CLI Enhancement ============
rich>=12.0.0
pyyaml>=6.0.0

# ============ Data Handling ============

# ============ PaddleOCR Mock (用於測試) ============
# 在 CI 環境中，我們 mock paddleocr，所以不需要真正安裝
