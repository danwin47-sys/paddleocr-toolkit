# CI/CD Testing Dependencies (不包含 paddlepaddle 以避免 CPU 兼容性問題)
# GitHub Actions 測試環境專用

# ============ PDF Processing ============
pymupdf>=1.24.0
pdf2image>=1.17.0
pillow>=10.4.0

# ============ Image Processing ============
opencv-python>=4.10.0
numpy>=1.26.0

# ============ Translation ============
ollama>=0.1.0

# ============ Web Framework (optional but for coverage) ============
fastapi>=0.115.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6
websockets>=12.0
pydantic>=2.0.0

# ============ CLI Enhancement ============
rich>=13.9.0
pyyaml>=6.0.0

# ============ Data Handling ============
python-docx>=1.1.0
markdown>=3.7.0

# ============ Development Dependencies ============
pytest>=8.3.0
pytest-cov>=4.1.0
pytest-env>=1.1.0
pytest-mock>=3.12.0
pytest-flask>=1.3.0
pytest-anyio>=0.0.0
black>=23.12.0
isort>=5.13.0
flake8>=7.0.0
mypy>=1.8.0

# ============ PaddleOCR Mock (用於測試) ============
# 在 CI 環境中，我們 mock paddleocr，所以不需要真正安裝
