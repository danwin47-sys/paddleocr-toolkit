# PaddleOCR Toolkit - Python Dependencies

# ============ Core OCR ============
# CI/CD 使用 CPU 版本（GitHub Actions 無 GPU）
# 本地開發可手動安裝 paddlepaddle-gpu==2.6.2
paddlepaddle==2.6.2
paddleocr==2.8.1

# ============ PDF Processing ============
pymupdf>=1.24.0

# ============ Image Processing ============
Pillow>=10.0.0
numpy>=1.24.0

# ============ Progress Bar ============
tqdm>=4.65.0

# ============ Excel Output (可選) ============
openpyxl>=3.1.0

# ============ Word Output (可選) ============
python-docx>=1.1.0

# ============ English Word Segmentation (可選) ============
wordninja>=2.0.0

# ============ Translation (可選) ============
requests>=2.31.0

# ============ Configuration ============
pyyaml>=6.0.0

# ============ Web API (v1.2.0新增) ============
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6

# ============ Rich UI (v1.2.0新增) ============
rich>=13.0.0

# ============ Development & Testing ============
pytest>=7.0.0
pytest-cov>=4.0.0
mypy>=1.0.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
