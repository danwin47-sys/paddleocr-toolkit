version: '3.8'

services:
  paddleocr-api:
    build: .
    container_name: paddleocr-toolkit
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./paddleocr_toolkit:/app/paddleocr_toolkit
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped
